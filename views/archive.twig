{# This file demonstrates using most of the index.twig template and modifying 
   just a small part. See `search.twig` for an example of another approach #}

{% extends "index.twig" %}

{% block content %}
<section class="bg-greylight">
	<div class="container">
        <form
            data-filter-form
            data-post-type="{{ post_type }}"
            {% if filter_url.base_url is defined and filter_url.base_url %}
                data-filter-url-base="{{ filter_url.base_url|e('html_attr') }}"
            {% endif %}
        >
            <input type="hidden" name="posts_per_page" value="{{ posts_per_page }}">
            <input type="hidden" name="col_class" value="{{ col_class }}">
			<div class="row mb-5">
				<div class="col-md-4 d-flex align-items-center">
					{{ breadcrumbs }}
				</div>
				<div class="col-md-8 d-flex align-items-center">
					<div class="w-100">
						<div class="row">
							<div class="col-sm d-flex align-items-center">
								{% if total is defined %}
								<p id="result-count" class="m-0" data-result-count>{{ total }} resultaten gevonden</p>
								{% endif %}
							</div>
							<div class="col-sm-auto d-flex align-items-center">
								{{ sort_select(filters.sort, {
									url_sync: false,
									layout: 'horizontal',
									options: {
										'relevance': 'Relevantie',
										'date_desc': 'Nieuwste eerst',
										'date_asc': 'Oudste eerst',
										'title_asc': 'Titel A-Z',
										'title_desc': 'Titel Z-A'
									}
								}) }}
							</div>
						</div>
					</div>
				</div>
			</div>
			
			{{ filter(filters.catagorie) }}
			
			{{ filter(filters.kleuren) }}
			
			<div class="row mb-4">
				<div class="col">
					<h4 class="m-0"> {{ title }} </h4>
				</div>
				<div class="col-auto">
					<button type="reset" class="btn btn-sm btn-outline-dark" data-filter-reset>
						Reset filters
					</button>
				</div>
			</div>

			<input type="text" name="s" class="form-control mb-4" placeholder="Zoek op trefwoord..." value="{{ filters.s.value|e }}">
			{{ active_filters({
				max_visible: 4,
				show_count: false
			}) }}

			<div class="position-relative">
				<div id="filter-loader" data-filter-loader class="filter-overlay d-none">
					<div class="spinner-border text-secondary" role="status" aria-hidden="true"></div>
				</div>
				
				<div id="filter-results">
					{% include 'partials/list.twig' %}
				</div>

				{% if current_page < max_num_pages %}
				<div class="text-center mt-4">
					<button type="button" class="btn btn-outline-dark" data-load-more>
						Laad meer
					</button>
				</div>
				{% endif %}
			</div>
		</form>
	</div>
</section>
{% endblock %}
