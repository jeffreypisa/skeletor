{% extends "base.twig" %}

{% block content %}
	<article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">	
		<section>
			<div class="container">
				<h1> {{ post.title }} </h1>
				{{ post.content }}
			</div>
		</section>
	</article>
{% endblock %}