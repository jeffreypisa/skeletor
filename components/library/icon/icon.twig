{#
Icon Component

Usage:
{{ icon({
    icon: 'arrow-down',
    style: 'light',
    library: 'fontawesome',
    class: 'arrow',
    icon_wrapper_height: 20,
    icon_wrapper_width: 20,
    icon_wrapper_class: '',
    title: 'More',
    url: 'https://example.com',
    title_position: 'right',
    title_level: 'span',
    title_class: '',
    icon_class: '',
    gap: 10,
    target: 'blank',
    color_primary: '',
    color_secondary: ''
}) }}

Parameters:
- icon (string, required): Naam van het icoon-bestand.
- style (string, optional): Stijlmap binnen de library. Standaard 'solid'.
- library (string, optional): Naam van de icon-library. Standaard 'fontawesome'.
- icon_wrapper_height (int|string, optional): Hoogte van de icon_wrapper. Standaard 30.
- icon_wrapper_width (int|string, optional): Breedte van de icon_wrapper. Standaard gelijk aan de hoogte.
- icon_wrapper_class (string, optional): Extra CSS-klassen voor de icon_wrapper.
- class (string, optional): Extra CSS-klassen voor het component.
- title (string, optional): Tekst bij het icoon.
- url (string, optional): Link om het icoon heen. Leeg laat alleen het icoon zien.
- title_position (string, optional): Positie van de tekst: 'left', 'right', 'top' of 'bottom'. Standaard 'right'.
- title_level (string, optional): HTML-tag voor de titel, zoals 'span', 'h2', 'h3'. Standaard 'span'.
- title_class (string, optional): Extra CSS-klassen voor de titel.
- icon_class (string, optional): Extra CSS-klassen voor het SVG-element.
- gap (int|string, optional): Ruimte tussen icoon en titel. Standaard 10.
- target (string, optional): Linkdoel, 'self' of 'blank'. Standaard 'self'.
- color_primary (string, optional): Kleur voor het primaire deel van een duotone icoon.
- color_secondary (string, optional): Kleur voor het secundaire deel van een duotone icoon.
#}
{% set tag = url ? 'a' : 'span' %}
{% set text_tag = title_level %}
<{{ tag }} class="icon icon--{{ title_position }} {{ class }}" style="gap: {{ gap }};"{% if url %} href="{{ url }}" target="{{ target }}"{% endif %}>
    {% if title_position in ['left','top'] and title %}
        <{{ text_tag }} class="icon__text {{ title_class }}">{{ title }}</{{ text_tag }}>
    {% endif %}
    <span class="icon__wrapper{{ icon_wrapper_class ? ' ' ~ icon_wrapper_class : '' }}"{% if icon_wrapper_width != 'auto' or icon_wrapper_height != 'auto' %} style="{% if icon_wrapper_width != 'auto' %}width: {{ icon_wrapper_width }};{% endif %}{% if icon_wrapper_height != 'auto' %} height: {{ icon_wrapper_height }};{% endif %}"{% endif %}>
        {{ svg|raw }}
    </span>
    {% if title_position in ['right','bottom'] and title %}
        <{{ text_tag }} class="icon__text {{ title_class }}">{{ title }}</{{ text_tag }}>
    {% endif %}
</{{ tag }}>
